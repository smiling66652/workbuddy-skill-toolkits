# 按需启用机制

> 整合后的 skill 不强制加载所有工具，而是按需启用，节省 tokens。

---

## 设计思路

```
一级：需求分析（无需加载任何工具）
  ↓
二级：根据需求，只加载需要的工具/references
  ↓
三级：执行
```

**实现方式**：SKILL.md 正文只写一级和二级的"选择逻辑"，具体工具的详细用法写在 `references/` 下，按需读取。

---

## SKILL.md 中的写法

```markdown
---
name: 联网工具箱
description: >
  统一联网操作工具箱（三级层级结构）：
  一级：分析用户需求（搜索/抓取/自动化/总结）
  二级：选择最佳工具（Playwright/Agent Browser/Human Browser/爬虫管道）
  三级：执行指令并返回结果。
  集成最佳工具：Playwright CLI、Agent Browser、Human Browser、MiniMax CLI。
---

# 联网工具箱

> **三级层级调用规则**：
> 1. **一级：需求分析** - 判断用户要做什么
> 2. **二级：工具选择** - 根据需求选择最佳工具
> 3. **三级：执行指令** - 调用工具并返回结果

---

## 快速选择指南

| 用户需求 | 推荐工具（二级选择） | 说明 |
|-----------|------------|------|
| 搜索信息、查资料 | **智能搜索** | 自动选引擎，无需指定 |
| 抓取网页内容、提取正文 | **网页抓取管道** | 自动降级，JS渲染兜底 |

---

## 一级：需求分析

**触发词分类**：

| 需求类型 | 触发词示例 | 跳转二级 |
|------------|------------|------------|
| 搜索 | 搜索、查一下、找资料 | [二级：智能搜索](#二级智能搜索) |

---

## 二级 + 三级：工具选择与执行

### 二级：智能搜索

**三级执行**：

```bash
# 方法1：使用 WebSearch 工具（优先）
# 直接调用 WebSearch，支持中英文引擎自动选择

# 方法2：使用 WebFetch 抓取搜索结果页
```

**详细用法**：详见 `references/智能搜索.md`
```

---

## references/ 按需读取

| 用户需求 | 读取的 reference 文件 | 是否加载工具 |
|------------|------------|------------|
| 搜索 | `references/智能搜索.md` | WebSearch / WebFetch |
| 抓取 | `references/网页抓取.md` | Playwright / requests |
| 浏览器自动化 | `references/浏览器自动化.md` | Playwright CLI / Agent Browser |

**效果**：只加载当前任务需要的工具和文档，节省 50-80% tokens。
