# 二级：IMA 笔记 —— 已降级为指针

> **2026-09-15 变更**：本文件原为 IMA 笔记操作的本地实现说明，经逐条比对（含铁律 1–4、路由速查表、
> 前置凭证、常用操作、跨模块任务）确认**全部被 `ima-skills` 覆盖**，且权威版更全，故降级为指针。

## 要做 IMA 笔记操作时

**加载 `ima-skills` 技能**（`C:\Users\Matebook\.workbuddy\skills\ima-skills\`）。

权威版已覆盖本副本的全部内容，并且**多**出：

- ⚠️ **PowerShell 5.1 GBK 静默转码坑**及规避写法（本机 Windows + PS 5.1，这条很关键）
- 内建每日更新检查（`-200` 码触发更新指引）
- 两层错误处理（`-100` 程序错误 / `-200` 需更新）
- 更完整的「易混淆场景」路由表

常用命令（与权威版一致）：
`import_doc`（新建笔记）、`append_doc`（追加）、`get_doc_content`（读取）、
`add_knowledge media_type=11`（关联到知识库）。

**跨模块任务**：同时涉及「笔记」与「知识库」时，必须同时读 `ima-skills` 的两个子模块说明。

> 原文件完整内容已备份至 `D:\_trash_\2026-09-15\知识工具箱-references\IMA笔记.md`。
